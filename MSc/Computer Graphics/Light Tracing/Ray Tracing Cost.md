## Brute Force
1 ray per pixel: Cost in $\mathcal{O}(N_{x}N_{y}N_{o})$: $\# pixels \times \# objects$.

Typically measured per ray: $\mathcal{O(N_{o})}$ per ray.

## Sub-linear Ray Tracing
(sub-linear per ray)