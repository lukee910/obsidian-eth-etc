Code reuse via extending a given class to a new one.

Often coupled with [[Subtyping]], but technically optional (see private inheritance in C++).

vs [[Aggregation]]: Only one object at runtime that combines functionality. Relation is fixed.
