An object is locally initialized if its non-null fields have non-null values.

Static type of an expression `e` is [[Non-Null Committed Type|committed]] $\Rightarrow$ `e`'s value at runtime is locally initialized.

vs [[Transitively Initialized]]
