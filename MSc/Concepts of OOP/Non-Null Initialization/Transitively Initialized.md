An object is transitively initialized if all reachable objects are [[Locally Initialized]].

Static type of an expression `e` is [[Non-Null Committed Type|committed]] $\Rightarrow$ `e`'s value at runtime is transitively initialized.
