Changing the method implementation of a supertype.

Can allow [[Contravariant]] parameters and [[Covariant]] return types, see [[03V Types pt. 2#Overriding]].

Note: Assume always has the same parameter count. Otherwise, hard to tell apart from [[Overloading]].
