Decompose a [[Vector Field]] in three parts with disticnt properties ([[Curl]] free, [[Divergence]] free, zero Laplacian):
![[Curl Helmholtz decomp..png]]

The [[Curl]] can tell us if the field is incompressible (which we want for fluids). Notably, check $\nabla \varphi$ for compressibility: 
